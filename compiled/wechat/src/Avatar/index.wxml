<wxs
  src="./index.wxs"
  module="utils" />

<view
  class="ant-avatar {{ className ? className : '' }}"
  style="{{ style }}">
  <image
    wx:if="{{ src }}"
    class="ant-avatar-image {{ utils.getClass(size) }}"
    src="{{ src }}" />
</view>
