.ant-skeleton-input {
  width: 400rpx;
  height: 40rpx;
  border-radius: 8rpx;
  background: var(--color-border, #eeeeee);
  display: inline-block;
}
.ant-skeleton-input-animate {
  background: linear-gradient(90deg, var(--color-border, #eeeeee) 25%, var(--color-text-weak-faded, rgba(204, 204, 204, 0.6)) 37%, var(--color-border, #eeeeee) 63%);
  background-size: 400% 100%;
  animation: ant-skeleton-loading 1.4s ease infinite;
}
