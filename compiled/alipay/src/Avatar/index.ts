import '../_util/assert-component2';
import { mountComponent } from '../_util/component';
import { AvatarDefaultProps } from './props';

const Avatar = () => {
  //
};

mountComponent(Avatar, AvatarDefaultProps);
