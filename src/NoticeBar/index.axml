<view 
  class="amd-v1-notice-bar {{className || ''}} {{type ? 'amd-v1-notice-bar-' + type : ''}}"
  a:if="{{show}}"
  style="{{style}}"
>
  <slot name="icon">
    <view className="amd-v1-notice-bar-icon" >
      <image-icon image="{{icon}}" a:if="{{ icon }}" class="amd-v1-notice-bar-icon-image"/>
      <icon type="InformationCircleOutline" a:elif="{{type ==='error'}}"/>
      <icon type="SoundOutline" a:else/>
    </view>
  </slot>
  <view class="amd-v1-notice-bar-content amd-v1-notice-bar-content-{{$id}}">
    <!-- 文本内容展示区域 -->
    <view
      class="amd-v1-notice-bar-marquee amd-v1-notice-bar-marquee-{{$id}}"
      style="{{marqueeStyle}} display: {{enableMarquee ? 'inline-block' : 'block'}}"
      onTransitionEnd="onTransitionEnd">
      <slot />
    </view>
  </view>
  <view class="amd-v1-notice-bar-operation">
    <slot name="extra"/>
    <!-- icon 类型 -->
    <icon
      className="amd-v1-notice-bar-operation-icon"
      type="RightOutline"
      onTap="onTap"
      a:if="{{mode==='link'}}"
    />
    <icon
      className="amd-v1-notice-bar-operation-icon"
      type="CloseOutline"
      a:if="{{mode==='closeable'}}"
      onTap="onTap" 
    />
  </view>
</view>
