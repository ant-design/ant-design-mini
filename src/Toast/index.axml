<view 
  class="amd-v1-toast {{className}} {{icon || image || type ? 'amd-v1-toast-icon-wrapper' : ''}}" 
  a:if="{{show}}" 
  style="{{style}}"
>
  <view a:if="{{type}}" class="amd-v1-toast-normal">
    <loading 
      a:if="{{type === 'loading'}}" 
      type="mini" 
      height="40px"
    />
    <am-icon 
      a:elif="{{type === 'warning'}}" 
      type="ExclamationOutline" 
      className="amd-v1-toast-icon"
    />
    <am-icon 
      a:elif="{{type === 'error'}}" 
      type="CloseOutline" 
      className="amd-v1-toast-icon" 
    />
    <am-icon 
      a:elif="{{type === 'success'}}" 
      type="CheckOutline" 
      className="amd-v1-toast-icon" 
    />
  </view>
  <am-icon 
    a:elif="{{icon}}" 
    type="{{icon}}" 
    className="amd-v1-toast-icon"
  />
  <view 
    a:elif="{{image}}" 
    style="background-image: url({{image}})" 
    class="amd-v1-toast-image" 
  />
  <view class="amd-v1-toast-text-body">
    <view class="amd-v1-toast-text-box">
      <view class="amd-v1-toast-text-content">
        {{content.substring(0, 24)}}
      </view>
    </view>
  </view>
</view>

<mask 
  className="amd-v1-toast-mask"
  a:if="{{show && showMask}}"
  show="{{true}}"
  onMaskTap="handleClickMask" 
  style="{{maskStyle}}" 
/>