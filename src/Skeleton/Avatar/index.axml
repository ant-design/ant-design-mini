<import-sjs from="./index.sjs" name="utils" />

<view
  a:if="{{loading}}" 
  style="{{style}}" 
  class="amd-skeleton-avatar amd-skeleton-avatar-{{shape}} {{animate?'amd-skeleton-avatar-animate':''}} {{utils.getClass(size)}} {{className || ''}}" 
/>
<slot a:else/>
