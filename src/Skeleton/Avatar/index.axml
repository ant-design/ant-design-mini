<view a:if="{{loading}}" style="{{size?('width:'+size+';height:'+size):''}}" class="amd-skeleton-avatar amd-skeleton-avatar-{{shape}} {{animate?'amd-skeleton-avatar-animate':''}} {{className||''}}" style="{{style}}">


</view>
<slot a:else/>