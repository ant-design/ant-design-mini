<view 
  a:if="{{loading}}" 
  style="{{size?`width: ${size}px; height: + ${size};`:''}}{{style || ''}}" 
  class="amd-skeleton-avatar amd-skeleton-avatar-{{shape}} {{animate?'amd-skeleton-avatar-animate':''}} {{className || ''}}" 
/>
<slot a:else/>