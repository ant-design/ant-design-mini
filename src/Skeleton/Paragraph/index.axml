<view a:if="{{loading && rows > 0}}" class="amd-v1-skeleton-paragraph {{animate?'amd-v1-skeleton-paragraph-animate':''}} {{className||''}}" style="{{style}}">
    <view class="amd-v1-skeleton-paragraph-row" a:for="{{rows}}"></view>
</view>
<slot a:else/>