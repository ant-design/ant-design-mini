<import-sjs from="./index.sjs" name="utils" />

<view class="amd-avatar {{className ? className : ''}}" style="{{style}}">
  <image
    class="amd-avatar-image {{utils.getClass(size)}}"
    src="{{src}}"
    a:if="{{src}}"
  />
</view>
