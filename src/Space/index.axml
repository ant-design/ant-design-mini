<!-- Space组件模板 -->
<import-sjs
  from="./index.sjs"
  name="_sjs" />

<view 
  class="ant-space {{ _sjs.getSpaceClass(direction, wrap, align, justify) }} {{ className || '' }}"
  style="{{ _sjs.getSpaceStyle(size) }}{{ style ? ';' + style : '' }}">
  <slot />
</view>
