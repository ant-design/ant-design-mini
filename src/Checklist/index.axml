<import-sjs from="./index.sjs" name="{getChecked}" />
<view class="amd-v1-checklist {{className ? className : ''}}" style="{{style || ''}}">
  <view class="amd-v1-checklist-body">
    <checklist-item
      a:for="{{options}}"
      checked="{{getChecked(item.value,mixin.value,multiple)}}"
      item="{{item}}"
      onChange="onChange"
    >
      <view slot="content" class="amd-v1-checklist-item-content-box-nut">
        <slot name="content" item="{{item}}">
          <image a:if="{{item.image}}" class="amd-v1-checklist-item-image" src="{{item.image}}" />
          <view class="amd-v1-checklist-item-text {{item.image ? '': 'amd-v1-checklist-item-text-no-image'}}">
            <view class="amd-v1-checklist-item-text-title {{item.disabled ? 'amd-v1-checklist-item-text-disabled' : ''}}">
              {{item.title}}
            </view>
            <view a:if="{{item.description}}" class="amd-v1-checklist-item-text-description {{item.disabled ? 'amd-v1-checklist-item-text-disabled' : ''}}">
              {{item.description}}
            </view>
          </view>
        </slot>
      </view>
      <view slot="icon">
        <slot name="icon">
          <icon type="CheckOutline" class="amd-v1-checklist-item-check-icon" />
        </slot>
      </view>
    </checklist-item>
  </view>
</view>