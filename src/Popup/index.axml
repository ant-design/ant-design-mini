<import-sjs from="./index.sjs" name="{getContentStyle}" />
<view
  class="amd-popup {{className||''}}"
  style="{{zIndex ? `z-index:${zIndex}` : ''}};{{style  || ''}}"
  a:if="{{visible||closing}}"
>
  <view class="amd-popup-mask {{maskClassName || ''}} {{closing&&!visible?'amd-popup-mask-closing':''}}" onTap="onTapMask" style="{{animation ? `-webkit-animation-duration:${duration}ms; animation-duration:${duration}ms;` : ''}} {{maskStyle || ''}}"/>
  <view
    class="amd-popup-content amd-popup-{{position}} {{closing?('amd-popup-'+position+'-close'):''}}"
    style="{{getContentStyle(position, animation, duration, width, height)}}"
    onAnimationEnd="onAnimationEnd"
  >
  <slot />
  <slot a:if="{{ isOldVersion }}" name="content"/>
  </view>
</view>