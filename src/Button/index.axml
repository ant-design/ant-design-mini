<import-sjs from="./index.sjs" name="utils" />


<button
  form-type="{{htmlType}}"
  hover-class="{{utils.getHoverClass(loading, type, activeClassName)}}"
  scope="{{scope}}"
  onTap="{{onTap ? 'onTap' : ''}}"
  catchTap="{{catchTap ? 'catchTap' : ''}}"
  public-id="{{publicId}}"
  open-type="{{openType}}"
  class="amd-v1-button {{inline ? `amd-v1-button-inline ${utils.getClass(size)}` : ''}} {{`amd-v1-button-${type}${danger ? '-danger' : ''}`}} {{(disabled || loading) ? 'amd-v1-button-disabled' : ''}} {{className ? className : ''}}"
  style="{{style}}"
>
  <view class="amd-v1-button-wrap">
    <block a:if="{{icon}}">
      <icon type="{{icon}}" />
    </block>
    <view class="amd-v1-button-content-text {{icon ? 'amd-v1-button-content-text-margin' : ''}}" >
      <slot />
      <view a:if="{{ !inline && subText}}" class="amd-v1-button-content-subtext">
        {{subText}}
      </view>
    </view>
    <view a:if="{{loading}}" class="amd-v1-button-content-loading-container">
      <loading
        type="mini"
        color="currentColor"
        className="amd-v1-button-content-loading"
      />
    </view>
  </view>
</button>
