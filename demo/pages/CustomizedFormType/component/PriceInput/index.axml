<view class="price">
  <input class="price-input" value="{{value.number || number}}" onInput="onNumberChange"/>
  <picker value="{{value.currency || currency}}" onChange="onCurrencyChange"  range="{{array}}">
    <text class="price-currency">{{value.currency || currency }}</text>
  </picker>
</view>