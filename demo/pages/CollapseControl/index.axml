<button onTap="toggle" inline>切换</button>
  <button onTap="changeContent" inline>修改content</button>
  <button onTap="addItems" inline>添加items</button>
  {{current}}

<collapse
  items="{{items}}"
  current="{{current}}"
  onChange="onChange"
/>
