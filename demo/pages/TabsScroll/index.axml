<!--basic-->
<tabs
  items="{{items}}"
  current="{{current}}"
  onChange="onChange"
>
  <icon slot="plus" type="AddOutline" />
  <view slot="content">
    {{items[current].content}}
  </view>
</tabs>

