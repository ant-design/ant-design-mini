<list header="基础使用">
  <list-item onTap="onTap">默认键盘</list-item>
  <rare-words-keyboard visible="{{visible}}" onClose="onTap"/>

  <list-item onTap="onTap2">不带蒙层</list-item>
  <rare-words-keyboard visible="{{visible2}}" showMask="{{ false }}" onClose="onTap2"/>
</list>
