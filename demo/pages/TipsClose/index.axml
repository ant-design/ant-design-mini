<button  
    type="primary" 
    onTap="onTap">
        {{visible === true ? '关闭' : '开启'}}
</button>

<tips 
    title="受控关闭"
    showClose="{{true}}" 
    onClose="onClose" 
    visible="{{visible}}"/>