<view class="ant-calendar">
  <block
    wx:for="{{ todoList }}"
    wx:for-index="taskIndex"
    wx:for-item="task">
    <!-- display: inline -->
    <text data-item-id="{{ taskIndex }}">{{ taskIndex }}{{ task }}</text>
  </block>
  <test loading />
</view>
