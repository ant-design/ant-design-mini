<view>
  <!-- display: inline -->
  <text a:if="{{ !!a }}">a</text>
  <!-- display: inline -->
  <text a:if="{{ !!a && b }}">a & b</text>
  <!-- display: inline -->
  <text a:if="{{ a }}">a</text>
  <!-- display: inline -->
  <text a:else>!a</text>
  <!-- display: inline -->
  <text a:if="{{ a }}">1</text>
  <!-- display: inline -->
  <text a:elif="{{ b }}">2</text>
  <!-- display: inline -->
  <text a:else>3</text>
  <text class="{{ a ? '1' : '2' }}" />
  <text class="1 {{ a ? '1' + '2' : 2 }} 2" />
  <view>{{ a || b }}</view>
</view>
