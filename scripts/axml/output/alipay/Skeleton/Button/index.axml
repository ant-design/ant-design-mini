<wxs
  src="./index.wxs"
  module="utils"
></wxs>
<view
  wx:if="{{loading}}"
  class="ant-skeleton-button {{animate ? 'ant-skeleton-button-animate' : ''}} {{utils.getClass(size)}} {{className || ''}}"
  style="{{style}}"
></view>
<slot wx:else></slot>